<div>
  <% if notice.present? %>
    <div class="container">
      <div class="row">
        <div class="col-md-10 offset-md-1 col-lg-8 offset-lg-2">
    <p><%= notice %></p>
  <% end %>
  <h1>ログイン後のページ</h1>
  <% current_user = User.find(session[:user_id]) %>
  <%= "現在ログイン中のユーザーのID:#{current_user.id}" %>
  <div class='text-center'>
    <%= link_to "LINEのログアウト", logout_path %>
  </div>
  <% if User.present? %>
    <%= User.count %>
  <% end %>
</div>