<div class="container pt-3">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <!-- 検索フォーム -->
      <h4>歯ブラシ一覧</h4>
      </div>
  </div>
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @toothbrushes.present? %>
          <%= render @toothbrushes %>
        <% else %>
          <p>見つかりません</p>      
        <% end %>
      </div>
    </div>
  </div>
  <%= link_to 'ユーザー編集はこちら', edit_user_path(@user) %>
  <%= link_to '歯ブラシを登録する', toothbrush_search_path%>
</div>